import {
  RecoilRoot,
  atom,
  selector,
  useRecoilState,
  useRecoilValue,
} from "recoil";

const App = () => {
  return (
     <RecoilRoot>
         <h1>recoil</h1>
     </RecoilRoot>
  )
};

export default App;
